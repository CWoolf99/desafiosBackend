<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>Lista EJS</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
</head>
<body>
<div class="text-bg-info p-3">
    <h1>Productos</h1>
    <% if(existelista){ %>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Nombre</th>
                    <th scope="col">Precio</th>
                    <th scope="col">Imagen</th>
                </tr>
            </thead>
            <tbody>
                <%prods.forEach(function(producto) {%>
                    <tr>
                        <td><%=producto.nombre%></td>
                        <td><%=producto.precio%></td>
                        <td>
                            <img src=<%=producto.imagen%> class="img-thumbnail img-fluid" height="150" width="150">
                        </td>
                    </tr>
                <%});%>
            </tbody>
        </table>
        <% } else{ %>
            <div class="alert alert-danger" role="alert">No se han registrado productos</div>
            <% } %>
            <a href="/" type="button" class="btn btn-primary">Agregar nuevo producto</a>

</div>
</body>